apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-gossip-world-db
  namespace: gossip-world
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard